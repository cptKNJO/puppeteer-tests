{
  "private": true,
  "scripts": {
    "test": "jest",
    "test:debug": "HEADLESS=false jest --runInBand",
    "lint": "eslint .",
    "lint:fix": "npm run lint --fix"
  },
  "dependencies": {
    "faker": "^4.1.0",
    "jest": "^26.0.1",
    "mkdirp": "^1.0.4",
    "puppeteer": "^4.0.0",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "eslint": "^7.2.0",
    "eslint-config-airbnb-base": "^14.1.0",
    "eslint-plugin-import": "^2.20.2"
  }
}
